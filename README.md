# Облепиха VPN 🌿

Стильный и уютный лендинг для VPN сервиса.

## 📸 Подготовка изображений
Для корректной работы приложения создайте папку `images/` в корне проекта и поместите туда файлы:
- `images/logo.jpeg` — логотип (примерно 512x512)
- `images/hero-image.jpeg` — главная иллюстрация для первого экрана

Все пути к изображениям управляются через файл `constants/images.ts`.

## 🚀 Локальный запуск (Node.js)

1. **Установите зависимости:**
   ```bash
   npm install
   ```

2. **Запустите сервер для разработки:**
   ```bash
   npm run dev
   ```
   Приложение будет доступно по адресу: `http://localhost:3000`

3. **Сборка для продакшена:**
   ```bash
   npm run build
   ```
   Готовые файлы появятся в папке `dist/`.

## 🐳 Запуск через Docker

Мы используем многоэтапную сборку (Node.js для билда + Nginx для раздачи статики).

1. **Соберите образ:**
   ```bash
   docker build -t oblepiha-vpn .
   ```

2. **Запустите контейнер:**
   ```bash
   docker run -d -p 8080:80 --name oblepiha-app oblepiha-vpn
   ```
   Приложение будет доступно по адресу: `http://localhost:8080`

## 🛠 Технологии
- **React 19** + **TypeScript**
- **Vite** (быстрая сборка)
- **Tailwind CSS** (стилизация)
- **Nginx** (сервер для Docker)
- **Google Fonts** & **Material Symbols**
